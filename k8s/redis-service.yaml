apiVersion: v1
kind: Service
metadata:
  labels:
    app: blockbuster-redis
  name: blockbuster-redis
spec:
  ports:
  - port: 6379
    protocol: TCP
    targetPort: 6379
  selector:
    app: blockbuster-redis
  type: NodePort

