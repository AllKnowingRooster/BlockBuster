apiVersion: v1
kind: Service
metadata:
  labels:
    app: authentication-service-postgresql
  name: authentication-service-postgresql
spec:
  ports:
  - port: 5432
    protocol: TCP
    targetPort: 5432
  selector:
    app: authentication-service-postgresql
  type: NodePort
status:
  loadBalancer: {}
