spring.application.name=authenticationService
server.port=12000
spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST}:5432/blockbusterauthenticationservice?currentSchema=springschema
spring.datasource.username=${POSTGRES_USERNAME}
spring.datasource.password=${POSTGRES_PASSWORD}

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true


jwt.secret=brownhenlayingonthegroundlayinganegg
jwt.accessExpiration=3600000
jwt.refreshExpiration=86400000

redis.url=redis://${REDIS_HOST}:6379
redis.expiration=7200

